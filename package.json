{
  "name": "t4g-nestjs-fastify",
  "private": true,
  "version": "0.0.1",
  "description": "Tag 4 Gift Admin - NestJS + Fastify backend with Turborepo",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^1.13.4",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@10.8.2",
  "engines": {
    "node": ">=18.0.0"
  }
}